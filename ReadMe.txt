v3.9.1 → v3.16.1

変更
- カメラ欄のセルを長押しで移動させるさいの移動可能になるまでの処理を高速化
- AE書き出しでカラーキーをOFFに変更
- メニューに「背景画像」を追加
- コマンドによる画像出力機能追加
- 背景画像編集にプリセット追加
- 背景画像編集時にマウスドラッグによる調整できるように変更
- 印刷・出力画像で前後のページのメモが同じ欄内に被っていたら描画
- 印刷・出力画像のACTION欄・CELL欄の文字サイズを文字数により変化
- 全体メモを追加
- 背景画像のメニュー項目、編集ダイアログの変更
- 全体メモの表示が合わないため、拡縮のステップ値を10%から20%に変更
- 印刷の全体メモの描画条件を変更


修正
- メインウィンドウがサブディスプレイにある時に、一部のダイアログがメインディスプレイの端に表示されるのを修正
- 「中割をClipStudioに適用しない」のチェックがオンのままtdtsファイルに保存すると中割が消えて保存されるのを修正
- ヘッダを編集して決定後に次の入力欄に移動しなくなっていたのを修正
- コンボボックスをもつダイアログでコンボボックスにフォーカスがあるさいにEnterキーを押しても確定しなくなっていたのを修正
- 動画欄へのXDTSデータ読み込み時に先頭に「_]が付いているレイヤーも読み込んでいたのを修正
- 「レイヤー名変更」ダイアログを修正
- 印刷で複数ページにまたがっているメモの３ページ目以降が描画されていないのを修正
- 全体メモを削除すると強制終了するのを修正
- タイムテーブルを下までスクロールした状態で拡大縮小するとメモがズレて表示されるのを修正


------------------------------------------------------------------------------------------------------

v3.9.0 → v3.9.1

修正
- 設定ファイルが保存されていないのを修正


------------------------------------------------------------------------------------------------------

v3.8.0 → v3.9.0

変更
- メニュー「ツール」に「After Effectsのタイムリマップデータで不透明度を使用する」を追加


------------------------------------------------------------------------------------------------------

v3.1.0 → v3.8.0

変更
- 一覧表示でマウスでスクロールできるように変更
- CELL欄のコンテキストメニューにACTION欄と同じ「リピート」を追加
- 画像出力に「PNG出力」「PNG分離出力」を追加
- 「保存時にJPEG画像を出力」を「保存時に画像を出力」に変更して、PNG画像なども出力タイプに追加
- 印刷の「｜」の表示を変更
- メニュー「Clip Studio」に「インポート時にカメラ座標を読み込まない」を追加
- ディレクション欄を編集中にメニューを開くと編集確定されるように変更
- 新しくシートを作成するさい、シート追加ウィンドウのシート名を1つ前のシート名を引用するように変更
- メニュー「表示」に「CELL欄を非表示」を追加
- ディレクション欄を編集中、ショートカットキーでメニュー項目を実行されると編集確定されるように変更
- メニュー [ファイル] に [カメラ座標を保存しない] を追加して、「インポート時にカメラ座標を読み込まない」は削除
- メニュー [Clip Studio] に [xを空セルとして読み込む] を追加
- 画像出力にも3秒形式を追加
- メニュー「印刷」を出力フォーマットごとの２段形式に変更
- 「CELL欄を非表示」ボタンを追加
- メニュー「Clip Studio」に「中割をCLIP STUDIO PAINTに適用しない」を追加
- メモのフォントサイズのデフォルトを15に変更
- メモのフォントサイズ、不透明度の保持機能追加
- ACTION・CELL欄の最大列数を100、SOUND・CAMERA欄の最大列数を10に変更
- カメラワーク名の「Wave Glass S」を「WaveGlass S」に変更
- SOUND・CAMERA欄の最大列数を8に変更
- SOUND・CAMERA欄の複数レイヤー一括消去機能追加
- Direction欄が最大行数以上大きくならないように変更
- 「コマ削除」「全レイヤーのコマ削除」の処理を変更
- 「コマ挿入」「全レイヤーのコマ挿入」の処理を変更
- Direction欄が最大行数以上大きくならないように変更
- 一枚画像出力でサイズが大きすぎると表示がおかしくなっていたので、分割して出力するように変更
- 画像出力時のファイル名の連番をゼロ埋め4桁に変更
- フリーテキストの出力フォーマットを変更


修正
- tdtsファイル保存先選択ダイアログが「保存」ではなく「開く」タイプになっていたのを修正
- 「PNG分離出力」時に入力画像のみ裏面を出力
- PNG画像出力時、ダミーコマがグレーになっていなかったのを修正
- 印刷・画像出力で一部のカメラが描画されないのを修正
- CELL欄非表示ONで再起動するとすべてのレイヤーが表示されるのを修正
- サウンド欄の履歴が残らないのを修正
- ディレクション欄、秒数を除くヘッダー欄を記入しようとすると別ウインドウで表示されるのを修正
- ヘッダー欄で決定しても次の項目に移動しないのを修正
- SOUND欄の3列目にデータを印刷すると強制終了するのを修正
- カメラを設定した際にDirection欄に追加されるカメラワーク名が最大行数を超えて追加されていたのを修正
- 印刷・画像出力でカメラワーク「WXP」が描画されないのを修正
- 「簡易コンテ動画作成」の書き出し先選択ダイアログのタイプが間違っていたので実行できなくなっていたのを修正
- 「3秒印刷」でメモが違う箇所にも描画されていたのを修正